import styled from 'styled-components'
import {backgroundImage} from 'styled-system'

const BackgroundHeader  = styled(backgroundImage)`
 background-attachment: fixed;
`
export default BackgroundHeader
